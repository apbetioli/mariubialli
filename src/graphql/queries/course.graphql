query Course($slug: String!) {
  course(where: { slug: $slug }) {
    title
    slug
    description
    image {
      url(transformation: { image: { resize: { width: 400 } } })
    }
    price
    checkoutURL
  }
}
